function printGender(){
    let color = "brown"

if(genderType === `Female`){
    var age = 30
    let color = "pink"
    console.log("Color inside the block:",color);
    
 }
 console.log("Age from Ifblock:", age);
 
}
 

genderType = `Female`
printGender(console.log("GenderType Globally:",genderType)
)
